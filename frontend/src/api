export const sendFrame = async (formData) => {
  try {
    const res = await fetch("http://localhost:8000/api/emotion/detect", {
      method: "POST",
      body: formData,
    });
    return await res.json();
  } catch (err) {
    console.error(err);
    return { error: "Failed to connect" };
  }
};
